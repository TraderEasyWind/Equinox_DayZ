modded class MRTs_C4: MRTsRemoteC4_Base
{
    override void ExplodeThis()
    {
        
        Explode( DT_EXPLOSION );    // Uses CfgAmmo for the kind of c4 being detonated
        
        // Necessary because instant delete will prevent explosion
        SetPosition( vector.Zero );
        timerDeleteThis = new Timer;
        timerDeleteThis.Run( 0.5, this, "YourMom" );
    }
    
    void YourMom()
    {
        //GetGame().ObjectDelete( this );
    }
}